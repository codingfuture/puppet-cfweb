<% |
    String $site,
    String $upstream,
    Array[String[1]] $locations,
|

$locs = $locations + ['@http']
-%>

<% $locs.each |$l| { -%>
    location <%= $l %> {
        proxy_pass http://<%= $upstream %>;
        include /etc/nginx/cf_http_params;
    }
<% } -%>
